# 测试bug记录（共4个）  ✅ **全部已修复**
## 发现时间：2026-07-08
## 修复时间：2026-07-08

---
## 1. 快照测试：test_default_session_id_format ✅
### 问题描述：
测试用例只mock了datetime.now()的返回值，但没有设置isoformat()方法的返回值，导致第二个datetime.now()调用（用于生成timestamp字段）返回的MagicMock对象无法被JSON序列化。
### 修复方案：
为 `mock_now.isoformat` 设置返回值（测试层修复）。
### 改动文件：
- [tests/sandbox/test_snapshot.py:269-273](tests/sandbox/test_snapshot.py#L269-L273)

---
## 2. 技能处理器测试：所有测试setup失败 ✅
### 问题描述：
ProcessContext构造函数的参数名是`arguments`，但测试中使用了`tool_args`，导致参数不匹配错误。
### 修复方案：
将测试中的`tool_args`改为`arguments`。
### 改动文件：
- [tests/skills/test_processors.py:21](tests/skills/test_processors.py#L21)

---
## 3. Cron调度器测试：所有任务执行测试失败 ✅
### 问题描述：
测试中使用了不存在的`called_once()`断言方法，当前Python版本的unittest.mock没有这个方法。
### 修复方案：
使用 `assert mock.call_count == 1` 代替 `assert mock.called_once()`。
### 改动文件：
- [tests/scheduler/test_cron.py:110](tests/scheduler/test_cron.py#L110)
- [tests/scheduler/test_cron.py:160](tests/scheduler/test_cron.py#L160)
- [tests/scheduler/test_cron.py:171](tests/scheduler/test_cron.py#L171)

---
## 4. Cron调度器测试：test_check_jobs_skips_disabled_jobs/test_check_jobs_skips_not_due_jobs ✅
### 问题描述：
同样使用了不存在的`not_called()`断言方法。
### 修复方案：
使用 `assert mock.call_count == 0` 代替 `assert mock.not_called()`。
### 改动文件：
- [tests/scheduler/test_cron.py:128](tests/scheduler/test_cron.py#L128)
- [tests/scheduler/test_cron.py:146](tests/scheduler/test_cron.py#L146)

## 验证结果
修复后测试套件状态：
- ✅ `tests/sandbox/test_snapshot.py`: 14/14 通过
- ✅ `tests/scheduler/test_cron.py`: 14/14 通过
- ✅ `tests/skills/test_processors.py`: 6/6 通过
